subprojects {
  apply plugin: 'java'

  repositories {
    jcenter()
  }

dependencies {
  // Pick one:

  // 1. Use Guava in your implementation only:
  implementation("com.google.guava:guava:28.2-jre") 
}  
}
